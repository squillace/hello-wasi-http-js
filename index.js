import { incomingHandler, types as types$1 } from '@bytecodealliance/preview2-shim/http';

const { handle } = incomingHandler;
const { Fields,
  FutureIncomingResponse,
  IncomingBody,
  Incoming,
  OutgoingRequest,
  RequestOptions } = types$1;
const { Error: Error$1 } = error;
const { Pollable } = poll;
const { InputStream,
  OutputStream } = streams;

export const incomingHandler = {
  handle(request, response) {
    
  },
};
